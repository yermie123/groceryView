import NavBar from "../NavBar/NavBar";

export default function MyList(){
    return(
        <div>
            <NavBar />
            <div id="mylistpage">
                <p>This is where the list content goes depending on what you chose to track</p>
            </div>
        </div>
    )
}